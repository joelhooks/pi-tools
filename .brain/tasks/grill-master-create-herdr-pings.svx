---
title: "Task: Create herdr-pings repo and extract the wake loop"
type: "grill-master_asset"
created_at: "2026-07-12"
---

# Task: Create herdr-pings repo and extract the wake loop

You are resolving the question `.brain/projects/grill-master/create-herdr-pings-repo.svx` — read it and the map's Notes (`.brain/projects/grill-master/herdr-pings-map.svx`) first. Joel approved the public repo name.

Execute the ticket precisely. Additional constraints:

- Work in pi-tools with surgical git: stage exact paths only, never `git add -A`. Two commits here: (1) nothing — the move is a removal: `git rm -r herdr-turn-ping herdr-ping-wait` + README pointer line, one commit "chore: herdr-turn-ping + herdr-ping-wait moved to joelhooks/herdr-pings". Push it.
- New repo at `~/Code/joelhooks/herdr-pings`: `gh repo create joelhooks/herdr-pings --public --clone` (description in the ticket). Copy the two directories' contents in BEFORE removing from pi-tools (copy, verify, then remove). Root layout: `herdr-turn-ping/`, `herdr-ping-wait/`, `README.md`, MIT `LICENSE` (Joel Hooks). Initial commit + push.
- README is public: spool contract summary, install (pi extension symlink one-liner, CLI symlink one-liner), no tailnet/machine/Brain references.
- Re-point live symlinks with `ln -sfn` to the new checkout; `ls -ld` receipts for both.
- Update the two path references to `pi-tools/herdr-turn-ping|herdr-ping-wait` in `~/.claude/skills/herdr-orchestration/SKILL.md` → new repo paths. Update `~/.claude/projects/-Users-joel/memory/herdr-wake-loop-standalone-repo.md` to say the extraction happened (keep frontmatter shape).
- Smoke per ticket: `cd /tmp && HERDR_PANE_ID='wT:p9' timeout 60 pi -p "say hi"` then confirm a new line appended to `~/.local/state/herdr-pings/wT-p9.jsonl`; `herdr-ping-wait wT:p9 --timeout 3` still exits per contract.
- Do NOT edit ticket files. Print `DONE: <repo URL + receipts gist>` when finished.
