---
title: "Task: Build restrained error notifications"
type: "wayfinder_asset"
created_at: "2026-07-12"
---

# Task: Build restrained error notifications

You are resolving `.brain/projects/wayfinder/build-error-notifications.svx` (in pi-tools; read it + the map Notes). Code lives in `~/Code/joelhooks/herdr-pings` — the bridge and actions you and a sibling worker built are now merged with the manifest at `herdr-plugin.toml` (`ff362ab`).

Execute the ticket. Constraints beyond it:

- Failures only: `turn_error` (pi extension path) and `pane_exited` (bridge path). Never on `turn_ended` or `pane_closed`.
- Mechanism per the ticket's lean: for `turn_error`, the pi extension (`herdr-turn-ping/index.ts`) shells `herdr notification show "<worker label> errored" --body "<error head, ~120 chars>" --sound request` fire-and-forget AFTER the spool append succeeds — notification failure must never affect the worker or the write. Skip when `HERDR_SOCKET_PATH` is absent. For `pane_exited`, add the same call in `bridge/lifecycle.js`.
- You may edit `herdr-turn-ping/index.ts` and `bridge/lifecycle.js` — no other worker is active in the repo now. Keep diffs minimal and in-style.
- Smoke: (1) forced mid-loop error — `HERDR_PANE_ID='wV:err2' OPENAI_API_KEY=sk-invalid pi --provider openai --model gpt-4o-mini -p hi` → one toast + one turn_error line; (2) normal `pi -p "say hi"` run → no toast; (3) kill a scratch pane you create → one toast. Paste transcript.
- Do NOT commit. Print `DONE: <gist + smoke transcript>` when finished.
